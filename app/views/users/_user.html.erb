<span class="p-r-2" style="white-space: nowrap;">

  <%= link_to user.name, user %>

  <span class="small text-muted">
    <%= user.company.name %>

    <% if !user.activated? %>
      not activated
    <% end %>

    <%= user.role unless user.role == "user" %>
  </span>

</span>
