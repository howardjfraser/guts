<% provide(:title, "Users") %>

<% if current_user.admin? %>
  <%= link_to "Add User", new_user_path, class: "btn btn-primary pull-xs-right" %>
<% end %>

<div class="m-b-1">
  <%= render partial: @users %>
</div>

<p class="text-muted">
  <%= pluralize @users.count, "user" %>

  <% if @invited.size > 0 %>
    plus <%= @invited.count %> waiting on activation -
    <%= render partial: @invited %>
  <% end %>
</p>
